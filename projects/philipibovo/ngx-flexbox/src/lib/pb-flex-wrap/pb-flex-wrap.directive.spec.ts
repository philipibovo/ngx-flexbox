import { PbFlexWrapDirective } from './pb-flex-wrap.directive';

describe('PbFlexWrapDirective', () => {
  it('should create an instance', () => {
    const directive = new PbFlexWrapDirective();
    expect(directive).toBeTruthy();
  });
});
