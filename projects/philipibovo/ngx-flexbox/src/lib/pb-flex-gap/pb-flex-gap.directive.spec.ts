import { PbFlexGapDirective } from './pb-flex-gap.directive';

describe('PbFlexGapDirective', () => {
  it('should create an instance', () => {
    const directive = new PbFlexGapDirective();
    expect(directive).toBeTruthy();
  });
});
