import { PbFlexOrderDirective } from './pb-flex-order.directive';

describe('PbFlexOrderDirective', () => {
  it('should create an instance', () => {
    const directive = new PbFlexOrderDirective();
    expect(directive).toBeTruthy();
  });
});
