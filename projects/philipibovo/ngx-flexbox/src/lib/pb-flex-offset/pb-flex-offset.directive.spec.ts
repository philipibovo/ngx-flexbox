import { PbFlexOffsetDirective } from './pb-flex-offset.directive';

describe('PbFlexOffsetDirective', () => {
  it('should create an instance', () => {
    const directive = new PbFlexOffsetDirective();
    expect(directive).toBeTruthy();
  });
});
