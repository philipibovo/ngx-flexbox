import { PbFlexSizeDirective } from './pb-flex-size.directive';

describe('PbFlexSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new PbFlexSizeDirective();
    expect(directive).toBeTruthy();
  });
});
