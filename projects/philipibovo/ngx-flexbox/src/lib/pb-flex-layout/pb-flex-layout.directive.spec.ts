import { PbFlexLayoutDirective } from './pb-flex-layout.directive';

describe('PbFlexLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new PbFlexLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
